import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2874f0',
      contrastText: '#fff',
    },
    secondary: {
      main: '#ffffff',
      contrastText: '#000',
    },
  },
});

export default theme;